import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Math Solver - STEMBridge",
};

export default function SolverLayout({ children }: { children: React.ReactNode }) {
  return children;
}
